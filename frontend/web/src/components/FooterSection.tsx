type FooterProps = {
    children: React.ReactNode
}

export const FooterSection = ({ children }: FooterProps) => {



    return (
        <div className="text-primary-white
                        
                      "
        >
          {children}
        </div>
    )
}